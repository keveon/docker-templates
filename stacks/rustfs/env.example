# ================================
# 基础配置
# ================================

# 镜像版本（可选，默认 latest）
# 建议使用具体版本号，如 1.0.0-alpha.76
# RUSTFS_VERSION=latest

# 时区
TZ=Asia/Shanghai

# 日志级别：trace, debug, info, warn, error
# RUSTFS_OBS_LOGGER_LEVEL=info

# ================================
# 服务配置
# ================================

# 数据存储路径
# 默认使用命名卷 data，如需使用路径绑定请设置绝对路径
# RUSTFS_DATA_PATH=/mnt/rustfs/data

# S3 API 监听地址
# RUSTFS_ADDRESS=:9000

# 控制台监听地址
# RUSTFS_CONSOLE_ADDRESS=:9001

# 启用 Web 控制台
# RUSTFS_CONSOLE_ENABLE=true

# ================================
# 认证配置（必填）
# ================================

# 访问密钥（生产环境必须修改）
RUSTFS_ACCESS_KEY=rustfsadmin

# 私有密钥（生产环境必须修改）
# 建议使用强密码，至少 8 个字符
RUSTFS_SECRET_KEY=rustfsadmin

# ================================
# 域名配置（可选）
# ================================

# 服务域名，用于虚拟主机样式访问
# 多个域名用逗号分隔
# RUSTFS_SERVER_DOMAINS=s3.example.com

# CORS 允许的来源域名
# 多个域名用逗号分隔，支持通配符 *
# RUSTFS_CORS_ALLOWED_ORIGINS=https://example.com
